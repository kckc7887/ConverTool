# ConverTool v1.2.0 发布说明

**契约包** `ConverTool.PluginAbstractions` 仍为 **1.1.0**，未变更。

---

## 新功能

### 1. 右键菜单集成

- 支持在资源管理器中通过右键菜单将文件发送到 ConverTool 作为输入
- 支持单选与多选文件的传入

## 修复

### 1. 插件安装后需要重启才生效

- 修复：安装插件后会立即重扫 `plugins/` 并刷新 UI（无需重启）

